{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Nœud de l'arbre des finances publiques",
  "type": "object",
  "required": [
    "id",
    "label",
    "statut",
    "enfants"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z]+(\\.[A-Za-z0-9_-]+)+$",
      "description": "Identifiant hiérarchique unique, ex. etat.depenses.RS.150"
    },
    "label": {
      "type": "string",
      "minLength": 3
    },
    "montant": {
      "type": [
        "number",
        "null"
      ],
      "description": "En euros. null si inconnu."
    },
    "annee": {
      "type": [
        "integer",
        "null"
      ],
      "description": "Millésime de la donnée (jamais mélanger silencieusement les années)."
    },
    "statut": {
      "enum": [
        "confirme",
        "estime",
        "inconnu"
      ],
      "description": "confirme = extrait tel quel d'une source officielle ; estime = ordre de grandeur à raffiner ; inconnu = la donnée n'existe pas publiquement"
    },
    "bloc_univers": {
      "type": [
        "string",
        "null"
      ],
      "description": "RACINE DE FICHIER UNIQUEMENT (ADR-0006). Code du sous-segment du dénominateur que cet arbre alimente (data/denominateurs/), ou null si l'arbre ne compte PAS dans la couverture C : vue transverse non sommée, agrégat estimé dont la couverture est portée ailleurs, ou volet dont le dénominateur n'est pas encore ouvert."
    },
    "niveaux": {
      "type": "array",
      "description": "RACINE DE FICHIER UNIQUEMENT (ADR-0006). Table de traduction « profondeur JSON → niveau de destination » sur l'échelle P0→P6. Un null signifie que ce cran n'avance PAS l'axe destination : l'euro conserve le dernier niveau non nul au-dessus de lui. C'est le cas d'une ventilation par nature de dépense (titre LOLF), qui dit comment l'argent est dépensé et non à qui il va.",
      "items": {
        "type": [
          "string",
          "null"
        ],
        "enum": [
          "P0",
          "P1",
          "P2",
          "P3",
          "P4",
          "P5",
          "P6",
          null
        ]
      }
    },
    "description": {
      "type": "string"
    },
    "source": {
      "type": "object",
      "properties": {
        "nom": {
          "type": "string"
        },
        "url": {
          "type": "string"
        },
        "producteur": {
          "type": "string"
        },
        "licence": {
          "type": "string"
        },
        "consulte_le": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
        },
        "maj": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date de dernière mise à jour du jeu de données source (affichée à côté du montant). null si non renseignée — à compléter."
        },
        "archive": {
          "type": [
            "string",
            "null"
          ],
          "description": "URL d'une copie archivée de la source (ex. Wayback Machine) — préservation contre les liens morts et preuve horodatée du contenu au moment de la contribution (issue #13)."
        }
      },
      "description": "Source officielle du chiffre. HÉRITAGE (ADR-0005) : un nœud sans source hérite de celle de son parent ; une source partielle (ex. juste `nom`) hérite des champs manquants. La racine du fichier doit être complète (nom, url, producteur, consulte_le). build.py résout l'héritage : chaque nœud généré porte sa source complète."
    },
    "inconnu": {
      "type": "object",
      "required": [
        "quoi",
        "contact"
      ],
      "properties": {
        "quoi": {
          "type": "string"
        },
        "contact": {
          "type": "string"
        },
        "url": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "a_completer": {
      "type": "object",
      "required": [
        "note"
      ],
      "properties": {
        "note": {
          "type": "string"
        },
        "url": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "enfants": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    }
  }
}
